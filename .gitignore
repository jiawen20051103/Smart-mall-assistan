# 依赖包（npm install 生成的，不用上传）
node_modules/
# 构建产物（npm run build 生成的，不用上传）
dist/
# 本地环境配置文件（可能含敏感信息，不用上传）
.env.local
.env.development.local
.env.production.local
# 日志文件
npm-debug.log*
yarn-debug.log*
# 编辑器缓存文件（比如 VS Code 的）
.vscode/
.idea/
# 操作系统文件（Mac 的 .DS_Store，Windows 不用管）
.DS_Store
# 其他临时文件
*.log